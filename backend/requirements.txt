# Backend service requirements. Install with:
#   uv pip install -r backend/requirements.txt
# The source ranges live in requirements.in; exact resolved versions are in
# constraints.txt. Regenerate with:
#   uv pip compile backend/requirements.in --python-version 3.11 --universal \
#     --output-file backend/constraints.txt --no-annotate
-r requirements.in
-c constraints.txt
